<h2>Edit Movie</h2>
<form action="/movies/{{movie._id}}" method="post">
    <div>
        <label for="title">Name : </label>
        <input id="title" name="title" type="text" value="{{movie.title}}">
    </div>
    <div>
        <label for="genre">Occupation : </label>
        <input id="genre" name="genre" type="text" value="{{movie.genre}}">
    </div>
    <div>
        <label for="plot">Catch Phrase : </label>
        <input id="plot" name="plot" type="text" value="{{movie.plot}}">
    </div>
     <div>
    <label for='cast'>Store: </label>
    <select name='cast' id='cast' multiple>
      <option selected disabled value='-1'>Select a cast</option>
      {{#each celebrities}}
        <option value='{{_id}}'>{{name}}</option>
      {{/each}}
    </select> 
  </div>
    <button>Update Celebrity</button>
</form>